<!-- #region header -->
<!-- Generated by @toolsync/builtin/package-readme. Do not edit manually, instead run `toolsync prepare`. -->

# @toolsync/template

[![NPM Version](https://img.shields.io/npm/v/@toolsync/template)](https://www.npmjs.com/package/@toolsync/template)

Update sections of source files

<!-- #endregion header -->

## Install

Use your favorite package manager to install this package:

```shell
pnpm install @toolsync/template # pnpm (recommended), you can also use npm, yarn, bun, whatever...
```

## Usage

> For command line usage, go ahead to [CLI](#cli)

Let's see how it works:

```ts
import { MarkdownTemplate } from '@toolsync/template';

// Load the current README.md as a template
// If it does not exist an empty string is assumed it's content
const readme = await Markdowntemplate.load('README.md');

// Update a section
readme.update({
  section: 'header', // ID of the section to update
  insert: 'top', // Insert the section on top if it does not exist in the file (otherwise throws)
  content: '# Hello!', // The content to insert. In this example, a markdown string is expected.
});

// Save the updated file back to README.md (you can also provide a different path)
await readme.save();
```

This will create/update a markdown file at `./README.md` with the following content:

```md
<!-- #region header -->

# Hello!

<!-- #regionend header -->

Other content...
```

Whenever you run the script again the section will be updated.

> Take a look at the source of this README, it's also updated with `@toolsync/template` :)

## CLI

<!-- #region cli-usage -->
<!-- This section is generated. Do not edit manually! -->

```
Usage: npx @toolsync/template [options] <file> <section> <content>

Update a section of a file with @toolsync/template

Arguments:
  file                   file to update (only .md files are supported)
  section                section to update
  content                content to insert into the section

Options:
  -V, --version          output the version number
  --notice <text>        notice to add to the section
  --insert <top|bottom>  where to insert the section if it does not exist
                         (default: "bottom")
  -h, --help             display help for command

For more information, visit https://toolsync.vercel.app
```

<!-- #endregion cli-usage -->

<!-- #region footer -->
<!-- Generated by @repo/toolsync-plugins. Do not edit manually, instead run `toolsync prepare`. -->

---

Part of the [toolsync](https://toolsync.vercel.app) project.

<!-- #endregion footer -->
